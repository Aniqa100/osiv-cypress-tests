#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../coffee-react-transform/bin/cjsx-transform" "$@"
  ret=$?
else 
  node  "$basedir/../coffee-react-transform/bin/cjsx-transform" "$@"
  ret=$?
fi
exit $ret
